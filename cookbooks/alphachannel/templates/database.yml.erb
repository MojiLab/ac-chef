general: &general
  adapter: postgresql
  min_messages: ERROR

development:
  <<: *general
  host: localhost
  username: <%= node['alphachannel']['db']['username'] %>
  database: <%= node['alphachannel']['db']['name'] %>

staging:
  <<: *general
  host: <%= node['alphachannel']['db']['host'] %>
  database: <%= node['alphachannel']['db']['name'] %>

production:
  <<: *general
  host: <%= node['alphachannel']['db']['host'] %>
  database: <%= node['alphachannel']['db']['name'] %>

test:
  <<: *general
  host: localhost
  database: ac_test
